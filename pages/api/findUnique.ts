import { NextApiRequest, NextApiResponse } from "next"
import prisma from "../../prisma/lib/prisma"

// export async function CarFindUnique(carid: string) {
//   const results = await prisma.car.findMany()
//   return results
// }
